<li>
  <%= gravatar_for user, size: 52 %>
  <%= link_to user.school_id.upcase, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "刪除", user, method: :delete, confirm: "確定?" %>
  <% end %>
</li>